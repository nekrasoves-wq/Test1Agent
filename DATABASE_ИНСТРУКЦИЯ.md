# Инструкция по использованию SQLite базы данных

## 📋 Что создано:

1. **database.py** - Менеджер базы данных с полным функционалом
2. **server.py** - Веб-сервер с интерфейсом для управления базой
3. **users.db** - Файл базы данных SQLite (создастся автоматически)

---

## 🚀 Быстрый старт:

### Вариант 1: Консольное использование

```bash
python database.py
```

Это запустит демонстрацию работы базы данных:
- Создаст таблицу
- Добавит тестовых пользователей
- Покажет все операции

### Вариант 2: Веб-интерфейс (РЕКОМЕНДУЕТСЯ)

```bash
python server.py
```

Затем откройте в браузере: **http://127.0.0.1:5000**

Вы увидите красивый веб-интерфейс с возможностью:
- ➕ Добавлять пользователей
- 🔍 Искать по имени/фамилии
- 📋 Просматривать всех пользователей
- 🗑️ Удалять записи

---

## 📦 Установка зависимостей:

Если Flask не установлен:

```bash
pip install flask
```

SQLite уже встроен в Python, дополнительная установка не требуется!

---

## 💻 Использование в коде:

```python
from database import DatabaseManager

# Подключаемся к базе
db = DatabaseManager('users.db')

# Добавляем пользователя
db.add_user('Иванов', 'Иван', 'Иванович')

# Получаем всех пользователей
users = db.get_all_users()

# Ищем пользователя
results = db.search_users('Иван')

# Обновляем данные
db.update_user(1, 'Петров', 'Пётр', 'Петрович')

# Удаляем пользователя
db.delete_user(1)

# Закрываем соединение
db.close()
```

---

## 🗄️ Структура базы данных:

**Таблица: users**

| Поле        | Тип       | Описание                    |
|-------------|-----------|------------------------------|
| id          | INTEGER   | Уникальный ID (автоинкремент)|
| last_name   | TEXT      | Фамилия (обязательно)        |
| first_name  | TEXT      | Имя (обязательно)            |
| middle_name | TEXT      | Отчество (опционально)       |
| created_at  | TIMESTAMP | Дата и время добавления      |

---

## 🔧 Доступные методы DatabaseManager:

- `add_user(last_name, first_name, middle_name)` - Добавить пользователя
- `get_all_users()` - Получить всех пользователей
- `get_user_by_id(user_id)` - Получить пользователя по ID
- `search_users(search_term)` - Поиск по имени/фамилии
- `update_user(user_id, last_name, first_name, middle_name)` - Обновить данные
- `delete_user(user_id)` - Удалить пользователя
- `get_user_count()` - Получить количество пользователей
- `display_all_users()` - Красиво вывести всех пользователей в консоль

---

## 📡 API Endpoints (для веб-сервера):

- **GET** `/` - Главная страница с интерфейсом
- **GET** `/api/users` - Получить всех пользователей (JSON)
- **POST** `/api/users` - Добавить пользователя (JSON)
- **DELETE** `/api/users/<id>` - Удалить пользователя
- **GET** `/api/users/search?q=<term>` - Поиск пользователей

---

## 🎯 Интеграция с вашим сайтом:

Чтобы связать ваш HTML сайт с этой базой данных:

1. Запустите сервер: `python server.py`
2. Измените в `index.html` URL отправки формы на: `http://127.0.0.1:5000/api/users`
3. Форма будет сохранять данные в локальную SQLite базу!

---

## ✨ Преимущества SQLite:

✅ Не требует установки сервера базы данных
✅ Все данные в одном файле (users.db)
✅ Быстрая работа
✅ Поддержка SQL запросов
✅ Встроена в Python
✅ Легко бэкапить (просто скопировать файл .db)

---

## 🔒 Безопасность:

База использует параметризованные запросы для защиты от SQL-инъекций.
Для production использования рекомендуется добавить:
- Валидацию данных
- Аутентификацию
- HTTPS
- Rate limiting

---

## 📝 Примеры использования:

### Добавление пользователя через Python:
```python
from database import DatabaseManager
db = DatabaseManager()
db.add_user('Сидоров', 'Сидор', 'Сидорович')
db.close()
```

### Получение всех пользователей:
```python
from database import DatabaseManager
db = DatabaseManager()
users = db.get_all_users()
for user in users:
    print(user)
db.close()
```

### Поиск пользователя:
```python
from database import DatabaseManager
db = DatabaseManager()
results = db.search_users('Иван')
for user in results:
    print(f"{user[1]} {user[2]} {user[3]}")
db.close()
```

---

Готово! Теперь у вас есть полноценная база данных! 🎉
